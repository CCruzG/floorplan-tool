# floorplan-prototype

A small Electron + Canvas floorplan prototype.  
Contains a renderer, drawing utilities, UI and a simple model.

Quick start
1. npm install
2. npm start

Saved floorplans use a 'duct_plan' style JSON with Plan_Boundary, Temperature_Regions, etc.
